import React from 'react'

export const SubHeader = (props) => {
    return (
        <div className="breadcrumbs full-width py-0">
            <div class="books_heading">
                <h1>{props.title}</h1>
            </div>

        </div>
    )
}
